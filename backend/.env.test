PORT=3001
NODE_ENV=test

# PostgreSQL — misma BD de desarrollo (los tests limpian solo datos @test.com)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=swrcrg_db
DB_USER=postgres
DB_PASSWORD=2304

# JWT
JWT_SECRET=
JWT_EXPIRES_IN=1h
